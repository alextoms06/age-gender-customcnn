# age-gender-customcnn

by default haar cascade by opencv- algorithm that identifies human features like eye-like, nose-like patterns
TRAINING- RGB images but webcam uses BGR by default, thus mismatch can occur.
